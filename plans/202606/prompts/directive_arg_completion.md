---
plan: sdd/plans/202606/directive_arg_completion.md
---
  Can you help me add auto-completion, for the prompt input widget and nvim (via the xprompt LSP server), that is triggered after the user types the ":" for one of the `%effort` or `%auto` directives' inputs? Both of these directives have inputs with a fixed set of valid values. Think this through thoroughly and create a plan using your `/sase_plan` skill. Submit your plan with the
`sase plan propose` command (as the skill instructs) before making any file changes.
 