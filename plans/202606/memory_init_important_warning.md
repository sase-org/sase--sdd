---
create_time: 2026-06-20 15:14:56
status: done
prompt: sdd/plans/202606/prompts/memory_init_important_warning.md
tier: tale
---
# Plan: Generate the SASE Plan Warning in `memory/sase.md`

## Context

`sase memory init` renders generated memory files through `src/sase/main/init_memory/roots.py`. The generated project
`memory/sase.md` already includes the ephemeral workspace section, but it does not include the existing project warning
that tells agents not to mention workspace directories in `/sase_plan` plan files.

## Implementation

1. Update the project-memory rendering path in `src/sase/main/init_memory/roots.py`, specifically the workspace-section
   helper, so project `memory/sase.md` files generated by `sase memory init` include the existing IMPORTANT warning
   immediately after the ephemeral workspace paragraph.
2. Keep the warning scoped to project memory only. Home-level generated memory does not get a project workspace section
   today, so it should not gain this project-specific paragraph.
3. Add focused assertions to the existing `sase memory init` tests in `tests/main/test_init_memory_handler.py` so a
   freshly generated project memory file contains the warning and home memory does not.

## Verification

1. Run the focused init-memory tests that cover generated project and home memory output.
2. Run `just install` if needed, then `just check` because this repo requires it after code changes.
3. Run `sase memory init` from the repo root and verify it reports no generated-memory changes. Confirm with
   `git status --short` that the command did not introduce any additional changes beyond the intentional source, test,
   and plan edits.
