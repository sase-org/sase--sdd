---
plan: sdd/plans/202604/instant_jk_navigation.md
---
 Can you help me do everything we possibly can (without breaking things) to make navigating the TUI using the `j`/`k` keymaps work so fast it appears to happen instantly at ALL times (regardless of what the user just did in
the TUI--ex: approved a plan, killed an agent, etc...)? This is a large piece of work that should be split into phases. I'll let you decide how many phases to create, but
keep in mind that each phase will be completed by a distinct agent instance (i.e. a distinct `claude` / `gemini` /
`codex` command). Think this through thoroughly and create a plan using your `/sase_plan` skill before making any file changes.

