---
plan: sdd/plans/202604/axe_rerun_done_bgcmd.md
---
 Can you help me add a new `r` (re-run) keymap to the "AXE" tab of the `sase ace` TUI that is only available when a done command entry is selected? This command should prompt the user (y/n) whether they want to dismiss the
command before re-running and should then re-run the command (creating a new entry on the AXE tab). Think this through thoroughly and create a plan using your `/sase_plan` skill before making any file changes.
