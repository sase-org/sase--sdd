---
plan: sdd/plans/202604/entry_jump_back.md
---
Can you help me add a new hidden apostraphe (`'`) hint to the hints that are shown when using the `'` keymap (which
shows hints next to the side-bar entries--works on all tabs)? If the user hits the `''` key twice, the idea is that they
should jump to the entry on that tab that they jumped from (they should be able to do this repeatedly to jump back and
forth from the same two entries). See how the hidden backtick hint on the "Jump to Entry" panel works for inspiration.
Think this through thoroughly and create a plan using your `/sase_plan` skill before making any file changes.
