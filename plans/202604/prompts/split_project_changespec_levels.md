---
plan: sdd/plans/202604/split_project_changespec_levels.md
---
   We currently group agent entries on the agents tab that are in the same agent group/tag panel by project / ChangeSpec (L1) and agent basename (L2). Can you help me split the project level (L1) from the  ChangeSpec level (L2)? This means that the agent name becomes the third level instead of the second. Note that, if there are no agents that target ChangeSpec, we shouldn't change the UI at all. Think this through thoroughly and create a plan using your `/sase_plan` skill before making any file changes.
