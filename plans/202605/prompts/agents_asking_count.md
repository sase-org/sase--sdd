---
plan: sdd/plans/202605/agents_asking_count.md
---
 Can you help me add a new agent count to the top of the the "Agents" tab of the `sase ace` TUI? This count should be named "asking" and it should include any agent that is currently waiting for human input (e.g. PLANNING). We already use a special icon to the left of the runtime in the agent row to visually mark these agents. Think this through thoroughly and create a plan using your `/sase_plan` skill before making any file changes.
