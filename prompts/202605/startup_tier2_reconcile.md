---
plan: sdd/tales/202605/startup_tier2_reconcile.md
---
 #resume:aik.2 The tier 2 full-history refresh should happen on startup at least, since a lot of agents that should be shown aren't otherwise. Can we keep all of the other changes, but still do one full refresh / reload of agent data once at startup time (i.e. right after `sase ace` starts)? Think this through thoroughly and create a plan using your `/sase_plan` skill before making any file changes.
