---
plan: sdd/epics/202605/docs_gpt_image_infographics.md
---
  Can you help me use GPT image to generate infographics for five markdown files (you choose which five based on which could use a diagram to illustrate the concepts the most) in the docs/ directory? This is a large piece of work that should be split into phases. I'll let you decide how many phases to create, but
keep in mind that each phase will be completed by a distinct agent instance (i.e. a distinct `claude` / `gemini` /
`codex` command). Think this through thoroughly and create a plan using your `/sase_plan` skill before making any file changes.

