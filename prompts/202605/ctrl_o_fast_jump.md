---
plan: sdd/tales/202605/ctrl_o_fast_jump.md
---
  Can you help me create a new `<ctrl+o>` keymap that should be a (faster I would expect--since we don't need to render UI hints) alias for the double-appostrophe keymap? Think this through thoroughly and create a plan using your `/sase_plan` skill before making any file changes.
