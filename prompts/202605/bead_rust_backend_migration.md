---
plan: sdd/plans/202605/bead_rust_backend_migration.md
---
  Can you help me make `sase bead` blazingly fast by migrating most of its logic to our Rust backend (sase-core)? This is a large piece of work that should be split into phases. I'll let you decide how many phases to create, but
keep in mind that each phase will be completed by a distinct agent instance (i.e. a distinct `claude` / `gemini` /
`codex` command). Think this through thoroughly and create a plan using your `/sase_plan` skill before making any file changes.

