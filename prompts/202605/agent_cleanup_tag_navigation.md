---
plan: sdd/tales/202605/agent_cleanup_tag_navigation.md
---
 When I use the `X` keymap to trigger the "Agent Cleanup" panel and then hit `t` to cleanup by tag, the panel that then opens does not respond to `j`/`k` navigation keys. Can you help me fix this? Think this through thoroughly and create a plan using your `/sase_plan` skill before making any file changes.
