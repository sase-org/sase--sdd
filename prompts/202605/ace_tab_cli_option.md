---
plan: sdd/tales/202605/ace_tab_cli_option.md
---
 Can you help me remove the logic we currently use to decide which tab to focus when the `sase ace` command is run? Let's add a new `--tab <tab_name>` option to `sase ace` where `<tab_name>` should only accept 3 values and should default to "agents" if the `--tab` option is not provided. Think this through thoroughly and create a plan using your `/sase_plan` skill before making any file changes.
