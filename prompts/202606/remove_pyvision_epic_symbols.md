---
plan: sdd/plans/202606/remove_pyvision_epic_symbols.md
---
 Can you help me remove all of the `--epic-symbol` options that are used for the pyvision target in Justfile? This epic is closed now, so these symbols should either be made private (if only used in that file), removed (if unused except for tests), or given an appropriate `# pyvision: ` pragma (if used externally by another GitHub repo). Think this through thoroughly and create a plan using your `/sase_plan` skill. Submit your plan with the `sase plan`
command (as the skill instructs) before making any file changes.
