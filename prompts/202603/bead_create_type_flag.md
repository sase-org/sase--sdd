---
plan: sdd/tales/202603/bead_create_type_flag.md
---
Can you help me migrate the `sase bead create` command's `--plan <plan_file>` and `--phase <bead_id>` to a single
`--type` option that accepts an argument of one of two forms: `plan(<plan_file>)` or `phase(<bead_id>)`. Think this
through thoroughly and create a plan using your `/sase_plan` skill before making any file changes.
