---
plan: sdd/tales/202604/agent_x_uses_rust_cleanup.md
---
 Can you help me make sure that dismissing a single agent using the `x` keymap on the Agents tab uses the same Rust core logic that is used by the `X` keymap to dismiss/kill the agent, since we
want this to be as fast as possible? Think this through thoroughly and create a plan using your `/sase_plan` skill before making any file changes.
