---
plan: sdd/tales/202604/panel_scoped_dismiss_kill.md
---
 Can you help me make it so the `X` keymap only attempts to dismiss agents in the same agent panel (i.e. that have the same agent tag) on the "Agents" tab of the `sase ace` TUI? Currently, we dismiss all done agents. We should
hmake a similar change to the `,X` keymap (which should kill/dismiss all agents in the current panel). Think this through thoroughly and create a plan using your `/sase_plan` skill before making any file changes.
