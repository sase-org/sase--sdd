---
plan: sdd/tales/202604/restore_t_tmux_agents_tab.md
---
 I think we accidentally got rid of the `t` keymap on the "Agents" tab of the `sase ace` TUI that opened a tmux window in the agent's claimed workspace, when we added the new `t` (add tag) keymap to that tab. Can you help me
restore the original `t` tmux keymap and change the trigger for the tag keymap from `t` to `N`? Think this through thoroughly and create a plan using your `/sase_plan` skill before making any file changes.
