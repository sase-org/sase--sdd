---
plan: sdd/tales/202604/rewind_bang_suffix.md
---
Can you help me add support for the `!` suffix when using the `R` (rewind) keymap on the "CLs" tab of the `sase ace`
TUI? This suffix should trigger similar behavior as when using with the `a` (accept) keyword: just update the ChangeSpec
as if the rewind has already been done (the user likely did it already manually). Think this through thoroughly and
create a plan using your `/sase_plan` skill before making any file changes.
