---
plan: sdd/tales/202604/bgcmd_background_launch.md
---
 When the user launches a background task on the AXE tab (i.e. uses the `,!` or `!!` keymaps), there can be a bit of a pause after the user hits enter. Can you help me start handling whatever slow operation that is causing
this using a background task (like we do with the `Y` keymap, which syncs a ChangeSpec, for example)? Think this through thoroughly and create a plan using your `/sase_plan` skill before making any file changes.
