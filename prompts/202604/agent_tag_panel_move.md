---
plan: sdd/plans/202604/agent_tag_panel_move.md
---
 After marking agent in the agents tab and then giving them a new tag (using the `N` keymap), the agent/workflow entries do not immediately move to their new panel (they do but it take a few seconds). Also, even after the
agents are moved to the new panel, they are still marked. They should be unmarked after being moved. Can you help me fix these issues? Think this through thoroughly and create a plan using your `/sase_plan` skill before making any file changes.
 