---
plan: sdd/tales/202604/agents_kill_focus_visible_order.md
---
 Now that the "Agents" tab of the `sase ace` TUI has deterministic ordering, when I use the `x` keymap to dismiss/kill agents, focus jumps to a random agent entry instead of to the entry below the one we killed/dismissed. Can
you help me fix this? Think this through thoroughly and create a plan using your `/sase_plan` skill before making any file changes.
